<% if obj.errors.any? %>
  <div class="alert alert-warning" role="alert">
    <h4 class="alert-heading"><%= pluralize(obj.errors.count, "error") %> prohibited this obj from being saved:</h4>
    <hr>
    <p>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </p>
  </div>
<% end %>