<% if current_user != user && user.name != "guestuser" %>
  <% if current_user.following?(user) %>
    <%= link_to destroy_relationship_path(user.id), method: :delete, class: "btn btn-info" do %>
      <i class="fa-solid fa-person-circle-minus"></i> フォロー解除
    <% end %>
  <% else %>
    <%= link_to  create_relationship_path(user.id), method: :post, class: "btn btn-success" do %>
      <i class="fa-solid fa-person-circle-plus"></i> フォローする
    <% end %>
  <% end %>
<% end %>
